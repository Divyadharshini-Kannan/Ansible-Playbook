---
- name: Setup web servers and deploy Flipkart clone
  hosts: webservers
  become: true
  connection: ssh
  tasks:

    - name: Install httpd
      ansible.builtin.yum:
        name: httpd
        state: present

    - name: Install git
      ansible.builtin.yum:
        name: git
        state: present

    - name: Ensure destination directory exists
      ansible.builtin.file:
        path: /home/ansible/flikartclone
        state: directory
        owner: ansible
        group: ansible
        mode: '0755'

    - name: Clone the Git repository
      ansible.builtin.git:
        repo: https://github.com/Msocial123/Flipkart-Clone.git
        dest: /home/ansible/flikartclone
        version: main
        force: yes
      become_user: ansible

    - name: Start and enable httpd service
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: true

